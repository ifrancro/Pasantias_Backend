# Perfil local opcional: ./mvnw spring-boot:run -Dspring-boot.run.profiles=local
# Misma política de esquema que producción (Flyway + validate).
# Para la PRIMERA adopción en una BD local ya poblada por ddl-auto=update:
#   export FLYWAY_BASELINE_ON_MIGRATE=true
#   export FLYWAY_BASELINE_VERSION=12
spring.jpa.show-sql=true
